@model TaskingSystem.Models.StudentsCourses

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>StudentsCourses</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mt-4">
                <label asp-for="StudentId" class="control-label"></label>
                <select asp-for="StudentId" class="form-control mt-2" asp-items="ViewBag.StudentId"></select>
                <span asp-validation-for="StudentId" class="text-danger"></span>

            </div>
            <div class="form-group mt-4">
                <label asp-for="CourseCode" class="control-label"></label>
                <select asp-for="CourseCode" class="form-control  mt-2" asp-items="ViewBag.CourseCode"></select>
                <span asp-validation-for="CourseCode" class="text-danger"></span>

            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary mt-4" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-link mt-4" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
